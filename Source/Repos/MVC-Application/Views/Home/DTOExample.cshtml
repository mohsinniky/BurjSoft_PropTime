@* @using MVC_Application.Models
@model List<StudentDto>
@{
    ViewData["Title"] = "DTOExample";
}


<h2>Students (DTO: Names Only)</h2>

<ul>
    @foreach (var student in Model)
    {
        <li>@student.Name</li>
    }
</ul> *@
